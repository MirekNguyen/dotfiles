function clean
  rm -rf "$HOME/Applications"
  rm -rf "$HOME/Movies"
  rm -rf "$HOME/Music"
  rm -rf "$HOME/.ansible"
  rm -rf "$HOME/.prettierd"
  rm -rf "$HOME/.kube"
  rm -rf "$HOME/.zsh_history"
  rm -rf "$HOME/.zcompdump"
  rm -rf "$HOME/.terraform.d"
  rm -rf "$HOME/.prettierd"
  rm -rf "$HOME/.vault-token"
  rm -rf "$HOME/.vscode"
  rm -rf "$HOME/.terminfo"
  rm -rf "$HOME/.expo"
  rm -rf "$HOME/.DS_Store"
  rm -rf "$HOME/.android"
  rm -rf "$HOME/.emulator_console_auth_token"
end
