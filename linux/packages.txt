# Packages installed on the Arch machine, one per line. `# ...` is a comment.
# Official repo and AUR packages live side by side: yay installs both.
#
#   linux/packages.sh install   install whatever here is missing
#   linux/packages.sh diff      compare this list with what is installed
#
# One-time setup that isn't a package (services, groups, pacman.conf, ...)
# is in SETUP.md.

# --- Installed by archinstall ------------------------------------------------
# Listed only so `diff` stays clean; everything below was installed by hand
# afterwards (see /var/log/pacman.log, which starts with the archinstall run).
base
linux
linux-firmware
amd-ucode                     # CPU microcode
mkinitcpio
sudo
efibootmgr
zram-generator                # compressed swap in RAM

# --- Core --------------------------------------------------------------------
networkmanager
openssh
git
base-devel                    # makepkg toolchain, needed to build yay
yay                           # AUR helper

# --- NVIDIA ------------------------------------------------------------------
nvidia-open                   # kernel modules
nvidia-utils
nvidia-settings

# --- Hyprland ----------------------------------------------------------------
hyprland                      # compositor
hypridle                      # screen off / lock on idle
hyprlock                      # lock screen
hyprpaper                     # wallpaper rotation
hyprpolkitagent               # password prompt for admin actions
hyprshot                      # screenshots (SUPER+SHIFT+3/4/5)
xdg-desktop-portal-hyprland   # screen sharing

# --- Wayland desktop ---------------------------------------------------------
waybar                        # top bar
swaync                        # notifications
swayosd                       # volume/brightness popup
ddcutil                       # monitor brightness over DDC/CI, see SETUP.md
rofi                          # launcher, bookmarks, clipboard picker
wofi                          # old launcher, replaced by rofi
wl-clipboard                  # wl-copy / wl-paste
cliphist                      # clipboard history
darkman                       # light/dark theme by sunrise and sunset
xremap-hypr-bin               # macOS-style shortcuts; needs the input group, see SETUP.md

# --- Fonts -------------------------------------------------------------------
otf-font-awesome              # waybar icons
ttf-iosevka-nerd              # terminal and bar font
noto-fonts-emoji              # color emoji

# --- Audio -------------------------------------------------------------------
pipewire-pulse                # PulseAudio compatibility for PipeWire
wireplumber                   # PipeWire session manager (wpctl)
alsa-utils
pavucontrol                   # right-click on the waybar volume

# --- Bluetooth ---------------------------------------------------------------
bluez
bluez-utils                   # bluetoothctl
overskride                    # click on the waybar bluetooth icon

# --- Terminal and shell ------------------------------------------------------
kitty
fish
fisher                        # fish plugin manager
starship                      # prompt

# --- Editor ------------------------------------------------------------------
neovim
tree-sitter-cli               # builds nvim-treesitter parsers

# --- CLI tools ---------------------------------------------------------------
fd                            # find
fzf                           # fuzzy finder
eza                           # ls
bottom                        # system monitor (btm)
yazi                          # file manager
gum                           # prompts in shell scripts
stow                          # symlinks dotfiles
zip
unzip

# --- Git and dev -------------------------------------------------------------
git-delta                     # diff pager
lazygit
github-cli                    # gh
opencode                      # coding agent
npm                           # for aicommit2 (lazygit commit messages), see SETUP.md

# --- VPN ---------------------------------------------------------------------
wireguard-tools               # wg-quick, home VPN
systemd-resolvconf            # lets wg-quick set the VPN's DNS
openconnect                   # work VPN client (work-vpn in work.fish)
vpn-slice                     # split tunnel for the work VPN

# --- Apps --------------------------------------------------------------------
helium-browser-bin            # browser
nautilus                      # file manager
gnome-calendar                # click on the waybar clock
proton-pass-bin               # passwords
localsend                     # AirDrop replacement
spotify
jellium-desktop-git           # Jellyfin client

# --- Chat --------------------------------------------------------------------
discord
telegram-desktop
teams-for-linux

# --- Games and streaming -----------------------------------------------------
steam                         # needs multilib, see SETUP.md
lutris                        # non-Steam games
sunshine-bin                  # game streaming host for Moonlight
